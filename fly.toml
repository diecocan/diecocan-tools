app = "diecocan-tools-staging"
primary_region = "iad"

[build]
    image = "ghcr.io/diecocan/diecocan-tools:latest"

[http_service]
    internal_port = 8080
    force_https = true
    auto_stop_machines = true
    auto_start_machines = true
    min_machines_running = 0