server {
    listen 80;

    location /v1/ {
        proxy_pass http://${BACKEND_HOST}:${BACKEND_PORT}/v1/;
    }

    location / {
        root /usr/share/nginx/html;
        try_files $uri /index.html;
    }
}