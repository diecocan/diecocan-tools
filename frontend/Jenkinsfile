pipeline {
    agent none

    stages {
        stage('Build and test') {
            agent {
                docker {
                    image 'node:22-alpine'
                    args '-v npm-cache:/home/node/.npm'
                }
            }
            steps {
                sh '''
                    npm ci
                    npm run lint
                    npm test
                '''
            }
            post {
                always {
                    junit 'reports/junit.xml'
                }
            }
        }
    }
}