pipeline {
    agent any

    stages {
        stage('HELLo') {
            steps {
                echo 'HELLo from diecocan-tools pipeline'
                sh 'pwd && ls -la'
            }
        }
    }
}